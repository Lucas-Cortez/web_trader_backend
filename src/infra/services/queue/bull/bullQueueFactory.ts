// import { QueueFactoryBase } from "core/contracts/queue-factory";
// import { BullQueue } from "./bullQueue";
// import { QueueConsumer } from "core/contracts/queue-consumer";
// import { QueueOptions } from "core/contracts/queue-options";

// export class BullQueueFactory implements QueueFactoryBase<BullQueue> {
//   public createInstance(key: string, consumer: QueueConsumer, options?: QueueOptions): BullQueue {
//     return new BullQueue(key, consumer, options);
//   }
// }
